<div class="container text-center">

    <div class="row">
        {{!-- Iteración sobre el arreglo de productos recibido desde el render --}}
        {{#each nProductos}}

        {{!-- Se llama a productos para que muestre cada elemento dentro del arreglo, pasandole el elemento como
        argumento --}}
        {{>productos
        productillo=this}}

        {{/each}}
    </div>
</div>